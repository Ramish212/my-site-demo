import type { Expense, Note } from "@/lib/types";
import {
  Book,
  Gift,
  Landmark,
  Plane,
  ShoppingCart,
  UtensilsCrossed,
} from "lucide-react";

export const totalBalance = 0;

export const expenses: Expense[] = [];

export const notes: Note[] = [];
